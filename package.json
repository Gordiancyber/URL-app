{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "URL-Shortener is a simple URL shortening service built using Node.js and Express. It provides a REST API to shorten URLs and redirect users to the original URLs. The project also includes a basic metrics API to track the usage of different domains.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2"
  }
}
